#!/usr/bin/env bash
# This script displays the part of the file names after the first dash in the current directory.
for file in *-*/; do
  # Extract the part after the first dash using parameter expansion
  echo "${file#*-}"
done
